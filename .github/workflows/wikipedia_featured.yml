name: Wikipedia Daily Scraper

on:
  schedule:
    - cron: "0 6 * * *"  # runs daily at 6:00 UTC (~1 AM EST)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape_wikipedia:
    name: Scrape and update Wikipedia daily
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install packages
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::rvest
            any::dplyr
            any::readr

      - name: Check out repository
        uses: actions/checkout@v4

      - name: Retrieve Wikipedia data
        uses: nick-fields/retry@v3
        with:
          command: Rscript -e 'source("get_wikipedia_daily.R")'
          continue_on_error: true

      - name: Commit updates
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: Update Wikipedia daily
          branch: main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
